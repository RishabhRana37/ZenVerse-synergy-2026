# scripts — utilities

- `fetch_<dataset>.(sh|py)` — one script per dataset (AIOps Challenge, Loghub); downloads into `data/raw/`. Any teammate must be able to reproduce the full data environment with these alone (matters for the finale rebuild drill).
- Replay/storm helpers and one-shot preprocessing tools.

Dataset details and links: `docs/DATASETS.md`.
