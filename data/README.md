# data

- `raw/` — downloaded originals via `scripts/fetch_*`. **Gitignored** — never commit.
- `samples/` — small curated slices, committed, enough for dev without downloads.
- `synthetic/` — generated storm scenarios (topology YAML + failure scripts), committed.

Sources, links, and preprocessing notes: `docs/DATASETS.md`.
